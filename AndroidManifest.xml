<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gov.polisen.ainappen"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_ainappen"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="gov.polisen.ainappen.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="gov.polisen.ainappen.Case"
            android:label="Ärende" >
        </activity>
        <activity
            android:name="gov.polisen.ainappen.Map"
            android:label="Karta" >
        </activity>
        <activity
            android:name="gov.polisen.ainappen.Information"
            android:label="@string/information" >
        </activity>
        <activity
            android:name="gov.polisen.ainappen.Contacts"
            android:label="@string/contacts" >
        </activity>
        <activity
            android:name="gov.polisen.ainappen.AddCase"
            android:label="@string/add_new_case" >
        </activity>
    </application>

</manifest>